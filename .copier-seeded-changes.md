# Seeded files the template changed

<!-- Generated by scripts/report_seeded_changes.py on every `copier update`;
     do not edit.  Files under `_skip_if_exists` are seeded once and never
     re-rendered, so this is the only place their template changes show up. -->

Template `https://github.com/pvliesdonk/fastmcp-server-template`: `v5.6.2` → `v6.1.0`.

5 seeded file(s) changed. Each is project-owned: apply what applies, keep your local content, and say in the update pull request what you did with each.

## `.gitignore`

````diff
--- previous/.gitignore
+++ target/.gitignore
@@ -43,9 +43,11 @@
 .mcpregistry_*
 .worktrees/
 docs/superpowers/
-# Everything under .claude/ is local EXCEPT skills/ — that subtree ships the
-# contributor-facing authoring skill(s), template-owned and re-rendered on
-# copier update (project additions live inside its DOMAIN-AUTHORING sentinel).
+# Provider-neutral contributor skills are tracked under .agents/.
+.agents/*
+!.agents/skills/
+# Everything under .claude/ is local EXCEPT skills/ — that subtree holds
+# symlinks into .agents/skills/, template-owned on copier update.
 .claude/*
 !.claude/skills/
 # No trailing slash: `.mcp.json/` would match a *directory* of that name and
````

## `.vale.ini`

````diff
--- previous/.vale.ini
+++ target/.vale.ini
@@ -6,7 +6,7 @@
 # pattern) and the CI step (`--glob='!docs/{superpowers,design,decisions}/**'`),
 # mirroring the mkdocs `exclude_docs:` block; Vale itself has no notion of
 # "skip this subtree", so the exclusion lives at the invocation site. See the
-# Documentation Conventions section in CLAUDE.md.
+# Documentation Conventions section in AGENTS.md.
 #
 # `MinAlertLevel = error` governs both what Vale prints AND its exit code —
 # warnings and suggestions are advisory (visible in `vale --minAlertLevel=
@@ -19,15 +19,24 @@
 # See https://vale.sh/docs/keys/format for the full key reference and
 # https://vale.sh/docs/topics/styles for rule-tuning syntax.
 #
-# Project-specific accepted vocabulary lives at
-# `.vale/styles/config/vocabularies/Base/accept.txt` (one term per line,
-# matched case-insensitively). The `Vocab = Base` line below activates it.
-# The config/ subtree is preserved across `vale sync`; see .gitignore for
-# the include/exclude rules.
+# Accepted vocabulary is layered: `.vale/styles/config/vocabularies/Base/
+# accept.txt` is this project's own list (seeded once, yours to grow), and
+# `.vale/styles/config/vocabularies/Template/accept.txt` is the template's
+# (re-rendered with the prose that needs it, so a term the template adds
+# arrives with the page that uses it). `Vocab = Base, Template` activates
+# both; one term per line, matched case-insensitively. The config/ subtree
+# is preserved across `vale sync`; see .gitignore for the include/exclude
+# rules.
+#
+# This file is seeded once (`_skip_if_exists`): copier writes it on the
+# first render and never rewrites it, so your rule tuning and package pins
+# are yours. The flip side: a change the template makes here later (a
+# vocabulary layer, a package URL) reaches you only by hand — the
+# `.copier-seeded-changes.md` report each `copier update` writes shows it.

 StylesPath = .vale/styles
 MinAlertLevel = error
-Vocab = Base
+Vocab = Base, Template

 # `Vale` is a built-in style, always available without listing here.
 # `Google`, `proselint`, `write-good` are upstream packages fetched by
@@ -37,11 +46,10 @@
 # reproducibility, append a version (e.g. `Google@1.0.0`).
 # `ai-tells` (https://github.com/tbhb/vale-ai-tells) catches common
 # LLM-prose tells; pinned to a specific release URL since it is not in
-# Vale's package index. NOTE: this file is `_skip_if_exists` — after the
-# first `copier copy`, copier updates no longer propagate URL changes here.
-# If tbhb/vale-ai-tells renames or removes the pinned release, `vale sync`
-# will fail permanently for this project. Periodically check
-# https://github.com/tbhb/vale-ai-tells/releases and bump the URL manually.
+# Vale's package index. This file is seeded once, so a bumped URL in the
+# template shows up in `.copier-seeded-changes.md` on the next update rather
+# than arriving here; if tbhb/vale-ai-tells renames or removes the pinned
+# release, `vale sync` fails until the URL is bumped by hand.
 #
 # THIS LINE IS THE SOURCE OF TRUTH for which packs exist, and three places
 # restate it by hand — change it and all three change with it:
@@ -53,9 +61,9 @@
 #      A pack missing there is never seen as missing, so the hook never
 #      syncs and the `vale` hook right after it fails on packaging.
 # In the TEMPLATE repo, CI asserts these three plus its own workflow's cache
-# list against this line, so drift there fails loudly. This file is
-# `_skip_if_exists` (see above), so your copy is yours: nothing in this
-# project's CI checks that the three above still match a pack you add here.
+# list against this line, so drift there fails loudly. This file is seeded
+# once (see above), so your copy is yours: nothing in this project's CI
+# checks that the three above still match a pack you add here.
 Packages = Google, proselint, write-good, https://github.com/tbhb/vale-ai-tells/releases/download/v1.9.0/ai-tells.zip

 [*.md]
````

## `.vale/styles/config/vocabularies/Base/accept.txt`

````diff
--- previous/.vale/styles/config/vocabularies/Base/accept.txt
+++ target/.vale/styles/config/vocabularies/Base/accept.txt
@@ -1,66 +1,9 @@
-# Project vocabulary — accepted tech terms that Vale's default spell-check
-# would otherwise flag as unknown words. One term per line. Vale matches
-# case-insensitively by default. See https://vale.sh/docs/topics/vocab.
+# Project vocabulary — accepted terms that Vale's default spell-check would
+# otherwise flag as unknown words in this project's own prose. One term per
+# line. Vale matches case-insensitively. See https://docs.vale.sh/keys/vocabularies.
 #
-# Add new accepted terms below as your docs corpus grows. Keep entries
-# alphabetical within their category for easy review.
-
-# --- Identity providers and auth-stack tooling ---
-Authelia
-Keycloak
-OAuth
-OIDC
-Traefik
-
-# --- HTTP / URL terminology ---
-hostname
-subpath
-URIs
-
-# --- OAuth / OIDC token fields ---
-access_token
-id_token
-
-# --- Python / runtime ecosystem ---
-dataclass
-debugpy
-lockfile
-mypy
-namespace
-namespaced
-pytest
-ruff
-scaffolded
-stdio
-uv
-venv
-venv's
-virtualenv
-
-# --- Operational / config jargon ---
-ADRs
-allowlist
-backports
-codecov
-config
-cron
-denylist
-deployer
-dev
-env
-hotfix
-knope
-repo
-ruleset
-rulesets
-systemd
-truthy
-uncommented
-uncommenting
-unprefixed
-upsert
-upserts
-validators
-
-# --- Diagnostics / observability ---
-traceback
+# This file is yours: copier seeds it once and never rewrites it. Terms the
+# template's own prose needs live in ../Template/accept.txt, which the
+# template maintains; `.vale.ini` activates both (`Vocab = Base, Template`).
+# Add project-specific terms below as your docs corpus grows, alphabetical
+# for easy review.
````

## `docs/releases/index.md`

````diff
--- previous/docs/releases/index.md
+++ target/docs/releases/index.md
@@ -1,7 +1,7 @@
 # Release Notes

 Each minor release series gets one page here telling its story: what
-changed, why it matters, and how to adopt it. Patch releases add a dated
+changed, why it matters, and how to adopt it. Patch releases add a versioned
 section to their series page, so a series stays one linkable document.
 The commit-level record lives in the repository's `CHANGELOG.md`; each
 GitHub release links back to its page here.
````

## `tests/test_smoke.py`

````diff
--- previous/tests/test_smoke.py
+++ target/tests/test_smoke.py
@@ -115,23 +115,36 @@
     assert json.loads(first.text)["server_name"] == "renamed-instance"


-def test_instructions_env_override(monkeypatch: pytest.MonkeyPatch) -> None:
-    """``SCHOLAR_MCP_INSTRUCTIONS`` replaces the built-in instructions.
-
-    ``build_instructions`` advertises this override in its hint line; honoring
-    it here makes that advertisement true instead of a dead hint.
-    """
+def test_instructions_env_override(
+    monkeypatch: pytest.MonkeyPatch, caplog: pytest.LogCaptureFixture
+) -> None:
+    """Legacy ``SCHOLAR_MCP_INSTRUCTIONS`` still replaces everything, and
+    says so: pvl-core logs one deprecation warning pointing at ``_EXTRA``."""
     monkeypatch.setenv("SCHOLAR_MCP_INSTRUCTIONS", "Custom operator text.")
-    assert make_server().instructions == "Custom operator text."
-
-
-def test_instructions_default_falls_back_to_build_instructions(
-    monkeypatch: pytest.MonkeyPatch,
-) -> None:
-    """Unset, instructions fall back to ``build_instructions`` (whose hint
-    advertises the override var). Guards the fallback arm of the override."""
+    # Scope to core's logger: make_server() re-applies FASTMCP_LOG_LEVEL to the
+    # root logger, which would otherwise drop the record under a stricter env.
+    monkeypatch.delenv("FASTMCP_LOG_LEVEL", raising=False)
+    with caplog.at_level("WARNING", logger="fastmcp_pvl_core"):
+        server = make_server()
+    assert server.instructions == "Custom operator text."
+    assert any(
+        "SCHOLAR_MCP_INSTRUCTIONS_EXTRA" in rec.getMessage() for rec in caplog.records
+    ), "expected the deprecation warning naming the _EXTRA replacement"
+
+
+def test_instructions_compose_identity_and_operator_extra(
+    monkeypatch: pytest.MonkeyPatch,
+) -> None:
+    """Unset ``INSTRUCTIONS``, the text is composed: the identity line the
+    scaffold contributes comes first, and ``_EXTRA`` is appended verbatim."""
     monkeypatch.delenv("SCHOLAR_MCP_INSTRUCTIONS", raising=False)
-    assert "SCHOLAR_MCP_INSTRUCTIONS" in (make_server().instructions or "")
+    monkeypatch.setenv("SCHOLAR_MCP_INSTRUCTIONS_EXTRA", "House rule: be brief.")
+    text = make_server().instructions or ""
+    assert text.startswith(
+        "FastMCP server for scholarly papers, patents, books and standards with docling PDF conversion",
+    )
+    assert "https://pvliesdonk.github.io/scholar-mcp/latest/llms.txt" in text
+    assert text.rstrip().endswith("House rule: be brief.")


 def test_blank_overrides_fall_back_to_defaults(
@@ -141,7 +154,7 @@

     ``env`` strips and treats a blank value as unset, so a blank SERVER_NAME
     must revert to ``scholar-mcp`` rather than rename the instance to
-    whitespace, and a blank INSTRUCTIONS must revert to ``build_instructions``.
+    whitespace, and a blank INSTRUCTIONS must leave the composed text in place.
     Guards against a future refactor (e.g. raw ``os.environ.get``) that would
     pass the blank value through.
     """
@@ -149,7 +162,9 @@
     monkeypatch.setenv("SCHOLAR_MCP_INSTRUCTIONS", "   ")
     server = make_server()
     assert server.name == "scholar-mcp"
-    assert "SCHOLAR_MCP_INSTRUCTIONS" in (server.instructions or "")
+    assert (server.instructions or "").startswith(
+        "FastMCP server for scholarly papers, patents, books and standards with docling PDF conversion",
+    )


 async def test_summarize_prompt_includes_context(client: Client[Any]) -> None:
````
