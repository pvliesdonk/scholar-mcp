# Seeded files the template changed

<!-- Generated by scripts/report_seeded_changes.py on every `copier update`;
     do not edit.  Files under `_skip_if_exists` are seeded once and never
     re-rendered, so this is the only place their template changes show up. -->

Template `https://github.com/pvliesdonk/fastmcp-server-template`: `v8.1.0` → `v8.2.0`.

3 seeded file(s) changed. Each is project-owned: apply what applies, keep your local content, and say in the update pull request what you did with each.

## `docs/design/reference/index.md`

````diff
--- previous/docs/design/reference/index.md
+++ target/docs/design/reference/index.md
@@ -0,0 +1,15 @@
+---
+okf_version: "0.2"
+---
+
+# External behavior references
+
+These pages record external behavior, with source markers and review dates.
+Read the relevant page before relying on its claims; research it again when
+its `stale_after` date passes. Project decisions belong in `docs/design/`.
+
+- [GitHub planning objects](github-planning-objects.md) — milestones,
+  issue relationships, and PR design material used by roadmapping and releases.
+
+See the [research log](log.md) for completed research passes. Add project
+references here as they are written.
````

## `docs/design/reference/log.md`

````diff
--- previous/docs/design/reference/log.md
+++ target/docs/design/reference/log.md
@@ -0,0 +1,10 @@
+# Reference research log
+
+## 2026-09-12
+
+- Added [GitHub planning objects](github-planning-objects.md), checked
+  against GitHub.com documentation and GitHub CLI 2.97.0. Covered milestone
+  identity, pagination, PR membership, nullable updates, issue hierarchy and
+  dependencies, and documented attachment support. Refute pass retained the
+  cross-owner REST documentation conflict and marked unreproduced UI/search
+  observations explicitly. Next review: 2027-03-12.
````

## `docs/design/roadmap.md`

````diff
--- previous/docs/design/roadmap.md
+++ target/docs/design/roadmap.md
@@ -0,0 +1,38 @@
+# Scholar MCP roadmap
+
+This is agent-authored synthesis, not a record of decisions. Tag each
+argument `stated` (the user's words), `derived` (synthesis), or
+`evidenced` (with a durable locator). Read the
+[`roadmapping` skill](../../.agents/skills/roadmapping/SKILL.md) before
+charting or refining work.
+
+Epics are parent issues for stories; packages are milestones for single
+release cuts. GitHub holds membership, dependencies and progress. This
+index holds the argument for the direction and order.
+
+## Direction
+
+Add one short entry per epic, linking its issue and frozen "Done when"
+outcome. Explain the ambition without inventing implementation detail.
+
+## Packages
+
+List intended cuts in order as `NNN name (major/minor/patch)`, each
+linked to its milestone with one sentence explaining the order. Use
+ordinals with gaps, such as `010` and `020`; kind is intent, and the
+release version is computed from what lands. Select "Alphabetical" on
+GitHub's Milestones page to see title order.
+
+Do not copy item lists or progress here. An issue with no milestone is
+backlog; commit it to a package when it should ship in that cut.
+
+## Known unknowns
+
+Give each unknown a resolved-by pointer to an issue or epic. When an
+answer changes what happens next and no planned work answers it, link a
+research issue with an appetite. Otherwise explain why recording it is
+enough.
+
+## Revisions
+
+Record dated changes of direction and their reasons here.
````
