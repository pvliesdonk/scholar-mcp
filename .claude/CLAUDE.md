# CLAUDE.md

<!-- Add your custom instructions below. Repowise will never modify anything outside the REPOWISE markers. -->
<!-- Examples: coding style rules, test commands, workflow preferences, constraints -->

<!-- REPOWISE:START — Do not edit below this line. Auto-generated by Repowise. -->
## IMPORTANT: Codebase Intelligence Instructions for scholar-mcp

> This repository is indexed by [Repowise](https://repowise.dev).
> Use the MCP tools below for orientation, discovery, and enriched context
> (documentation, ownership, history, decisions). **Always verify against
> actual source files before making changes** — the index may be stale.

Last indexed: 2026-06-02 (commit 1e73de3). Confidence: 100%.
### Architecture
repo is a scholarly research orchestration engine that ingests bibliographic queries and document identifiers, processes them through a multi-source retrieval pipeline (Semantic Scholar, OpenAlex, OpenLibrary, and Patent databases), and synthesizes enriched metadata and document content into structured MCP (Model Context Protocol) server responses. By abstracting complex API interactions and document parsing into a unified toolset, it enables LLMs to perform automated literature reviews, citation enrichment, and document analysis. - **Core Language:** Python (60.3%)
- **Documentation:** Markdown (22.9%)
- **Configuration & Infrastructure:** YAML (9.5%), Shell (3.9%), JSON (1.7%), TOML (1.1%)
- **Key Dependencies:** 
    - **MCP (Model Context Protocol):** Provides the communication layer for LLM-integrated tool execution. - **Docling:** Used for document parsing and content extraction.
### Key Modules
| Module | Purpose | Owner |
|--------|---------|-------|
| `community-0` | The tests module serves as the validation and quality assurance layer of the sch | — |
| `community-4` | The tests module serves as the validation and quality assurance layer of the sch | — |
| `community-1` | The scholar_mcp module acts as the standards-ingestion and knowledge-retrieval s | — |
| `community-5` | The scholar_mcp module serves as the retrieval and enrichment subsystem of the M | — |
| `community-2` | The scholar_mcp module serves as the core integration and transport adapter for  | — |
| `community-3` | The scholar_mcp module serves as the architecturally central transport adapter a | — |
| `community-6` | The scholar_mcp module serves as the transport adapter and service orchestration | — |
| `community-81` | The tests module serves as the verification and quality assurance layer of the s | — |
### Entry Points
- `src/scholar_mcp/server.py`
- `docs/guides/index.md`
- `docs/index.md`
- `docs/tools/index.md`
- `packaging/mcpb/src/server.py`
- `server.json`
### Tech Stack
**Languages:** Python
**Frameworks:** HTTPX

**Infra:** Docker### Architectural Layers
| Layer | Files | Purpose |
|-------|-------|---------|
| External Data Integrations | 31 | A collection of API clients and enrichment modules designed to fetch and process |
| MCP Server Orchestration | 14 | Core infrastructure for the Model Context Protocol server, managing synchronizat |
| Citation Processing Engine | 15 | Utilities for parsing, formatting, and resolving citation data, PDF links, and b |
| Server Tools Interface | 14 | Implementation of MCP server capabilities, including search, graph traversal, re |
| Infrastructure and Caching | 17 | Support utilities including rate limiting, data caching, and asynchronous task h |
| Scholar MCP Integration | 9 | Handles patent data retrieval, EPO client communication, and document parsing fo |
| Project Changelog | 1 | Maintains the historical record of project updates, versioning, and feature chan |
| Code Coverage Configuration | 1 | Defines settings and thresholds for automated test coverage reporting. |
| Project Build Configuration | 1 | Manages Python project dependencies, build system requirements, and package meta |
| Template Metadata | 1 | Stores configuration answers used by the Copier scaffolding tool to generate pro |

### Guided Tour (6 steps)
1. **Project Overview and Documentation** — `README.md`
2. **Server Entry Points** — `src/scholar_mcp/server.py`
3. **Core Data Models and Protocols** — `src/scholar_mcp/_record_types.py`
4. **External Data Integrations** — `src/scholar_mcp/_s2_client.py`
5. **Processing and Infrastructure** — `src/scholar_mcp/_cache.py`
6. **Testing and Verification** — `tests/conftest.py`
### Hotspots (High Churn)
| File | Churn | 90d Commits | Owner |
|------|-------|-------------|-------|
| `tests/test_standards_client.py` | 99.1th %ile | 26 | Peter van Liesdonk |
| `src/scholar_mcp/_standards_client.py` | 98.3th %ile | 24 | Peter van Liesdonk |
| `src/scholar_mcp/_cache.py` | 97.4th %ile | 24 | Peter van Liesdonk |
| `tests/test_tools_graph.py` | 96.5th %ile | 11 | Peter van Liesdonk |
| `tests/test_tools_patent.py` | 95.7th %ile | 15 | Peter van Liesdonk |

## Code health
Hotspot health: 5.24/10 (stable) ·
Average: 6.17/10 ·
Worst: 2.35/10 (`src/scholar_mcp/_citation_formatter.py`)

### Critical biomarkers
- `tests/test_epo_xml.py` — churn risk — impact −2.4
- `src/scholar_mcp/_cache.py` — untested hotspot — impact −2.0
- `tests/test_citation_formatter.py` — prior defect — impact −2.0
- `tests/test_openlibrary_client.py` — prior defect — impact −2.0
- `tests/test_tools_graph.py` — prior defect — impact −2.0

### Repowise MCP Tools

This repo has the Repowise MCP server configured. The tools below answer questions `grep`/`Read` cannot. Every response carries an `_meta` envelope with `index_age_days`, `indexed_commit`, and a `stale_warning` only when the index has actually diverged from HEAD — silence means the index is current.

**When to call which tool:**

| Tool | What only this tool answers |
|------|------------------------------|
| `get_answer(question)` | Synthesised answer with verified citations and a calibrated `retrieval_quality`. First call for "how does X work" / "why is Y like this". On low confidence returns `best_guesses` with one-line justifications instead of an empty answer. |
| `get_context(targets=[...])` | Triage card for files/modules/symbols — title, summary, signatures, `hotspot` bit, `decision_records` titles, and `symbol_id`s to pipe into `get_symbol`. Use `include=["callers","ownership",...]` to widen. NOT for source bytes. |
| `get_symbol("path/to/file.py::Name")` | Raw source bytes for one indexed symbol with exact line bounds. Cheaper and safer than `Read` + offset math. Use the `symbol_id` returned by `get_context`. |
| `search_codebase(query, kind?)` | Find pages by concept when you don't know the file. Each result carries `search_method` (`embedding` vs `bm25` fallback). For exact identifiers use Grep — the tool will hint when it sees one. |
| `get_why(query, targets?)` | Architectural decision archaeology — *why* the code is shaped this way. Call before refactors or pattern divergences. Falls back to git archaeology when no ADRs exist for a file. |
| `get_risk(targets, changed_files?)` | What history says about touching these files: churn, owners, blast radius. Pass `changed_files` for PR mode → returns a `directive` (`will_break`, `missing_cochanges`, `missing_tests`). |
| `get_dead_code(...)` | Tiered unreachable / unused-export / zombie-package findings. Run before a cleanup sprint, not before a targeted fix. |
| `get_overview(repo?)` | Architecture map for an unfamiliar repo. One-time orientation; skip on subsequent calls in the same session. |

**Composition tips:**
- `get_answer` → if `confidence` is `medium`/`low`, follow the `best_guesses[0].file` or `fallback_targets[0]` into `get_context`, then `get_symbol` for bytes.
- `get_context` returns `decision_records` titles → call `get_why(targets=[...])` for the rationale.
- `get_context` returns `hotspot: true` → call `get_risk` before editing.
- PR review → `get_risk(targets=[...], changed_files=[...])`; read the `directive` block first.

**Verify when:** `_meta.stale_warning` is present, or `retrieval_quality` is `partial`/`weak`, or `search_method` is `bm25`. Otherwise trust the response and act on it.

### Codebase Conventions
**Commands:**
- Test: `pytest`
- Lint: `ruff check .`
- Format: `ruff format .`
- Typecheck: `mypy .`

<!-- REPOWISE:END -->
