# Rendered at build time by envsubst. ${VERSION} matches the package release.
[project]
name = "scholar-mcp-mcpb"
version = "${VERSION}"
requires-python = ">=3.11"
dependencies = [
  "pvliesdonk-scholar-mcp[mcp]==${VERSION}",
]
