{
  "manifest_version": "0.4",
  "name": "scholar-mcp",
  "display_name": "Scholar",
  "version": "${VERSION}",
  "description": "Scholarly-sources MCP server for papers, patents, books, and standards.",
  "long_description": "Unified search, cross-reference, and retrieval across papers (Semantic Scholar + OpenAlex), patents (EPO OPS), books (Open Library), and standards (NIST, IETF, W3C, ETSI). Includes citation graph traversal, BibTeX/CSL-JSON/RIS generation, and optional docling-serve PDF-to-Markdown conversion.",
  "author": {
    "name": "Peter van Liesdonk",
    "url": "https://github.com/pvliesdonk"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pvliesdonk/scholar-mcp.git"
  },
  "homepage": "https://pvliesdonk.github.io/scholar-mcp/",
  "documentation": "https://pvliesdonk.github.io/scholar-mcp/",
  "support": "https://github.com/pvliesdonk/scholar-mcp/issues",
  "license": "MIT",
  "keywords": [
    "scholar",
    "semantic-scholar",
    "openalex",
    "epo",
    "patents",
    "open-library",
    "standards",
    "citations",
    "bibtex"
  ],
  "server": {
    "type": "uv",
    "entry_point": "src/server.py",
    "mcp_config": {
      "command": "uvx",
      "args": [
        "--from",
        "pvliesdonk-scholar-mcp[mcp]==${VERSION}",
        "scholar-mcp",
        "serve"
      ],
      "env": {
        "SCHOLAR_MCP_SERVER_NAME": "${user_config.server_name}",
        "FASTMCP_LOG_LEVEL": "${user_config.log_level}"
      }
    }
  },
  "user_config": {
    "server_name": {
      "type": "string",
      "title": "Server name",
      "description": "Rename this server instance; defaults to the project name.",
      "required": false
    },
    "log_level": {
      "type": "string",
      "title": "Log level",
      "description": "Log level for FastMCP internals and app loggers (DEBUG / INFO / WARNING / ERROR / CRITICAL). The -v CLI flag overrides to DEBUG.",
      "required": false,
      "default": "INFO"
    }
  },
  "compatibility": {
    "claude_desktop": ">=0.10.0",
    "platforms": [
      "darwin",
      "win32",
      "linux"
    ],
    "runtimes": {
      "python": ">=3.11,<4.0"
    }
  }
}
